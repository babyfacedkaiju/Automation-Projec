# Automation Project

A small toolkit of Python automation utilities: file organizer and bulk renamer.

## Features
- Organize files into folders by extension
- Bulk rename files with prefix/suffix
- Small unit test included

## Tech
- Python 3.8+
- pytest (for tests)

## How to run

1. Create virtualenv (PowerShell):
```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
